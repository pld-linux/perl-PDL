--- PDL-2.2.1/Lib/FFTW/Makefile.PL.orig	Fri Apr 21 08:23:55 2000
+++ PDL-2.2.1/Lib/FFTW/Makefile.PL	Fri May 11 03:35:00 2001
@@ -151,7 +151,7 @@
 		# make a list of libs from the libstring
 		my @libs = split ' ',$double_lib;
 		grep s/\-l//g, @libs;
-		grep $_ = "lib$_.a", @libs;
+		grep $_ = "lib$_.so", @libs;
 		my %libs;
 		my $lib;
 		foreach $libdir(@libdirs){
